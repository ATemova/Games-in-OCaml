(* Mastermind game with feedback analysis *)

let generate_code () = 
  (* Generate a random code *)
  [1; 2; 3; 4]

let guess_code code guess =
  (* Return feedback on guess *)
  (0, 0)

let play_game () =
  let code = generate_code () in
  (* Implement game loop with feedback analysis *)
  ()
